(set-option :produce-proofs true)
(set-logic QF_BOOL)
(declare-const p Bool)
(declare-const q Bool)
(assert (or p q))
(check-sat)
(push 1)
(assert (not p))
(check-sat)
(assert (not q))
(check-sat)
(get-proof)
