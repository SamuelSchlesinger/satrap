(set-option :produce-proofs true)
(set-logic QF_BOOL)
(declare-const p Bool)
(declare-const q Bool)
(assert (! (=> p q) :named implication))
(push 1)
(assert p)
(assert (not q))
(check-sat)
(get-proof)
