(set-option :produce-proofs true)
(set-logic QF_UF)
(declare-sort U 0)
(declare-const a U)
(declare-const b U)
(declare-const c U)
(declare-const condition Bool)
(declare-fun f (U) U)
(declare-fun predicate (U) Bool)
(declare-fun choose (Bool U) U)
(define-fun twice ((x U)) U (f (f x)))
(assert (= a b))
(assert (= b c))
(assert condition)
(assert (= (predicate a) (predicate b)))
(assert (= (choose condition a) (choose true b)))
(assert (= (ite condition a c) a))
(assert (= (twice a) (f (f b))))
(check-sat)
(push 1)
(assert (distinct (f a) (f b)))
(check-sat)
(get-proof)
