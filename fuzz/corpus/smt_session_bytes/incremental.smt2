(set-option :produce-unsat-cores true)
(set-logic QF_UFLIA)
(declare-const x Int)
(declare-const y Int)
(declare-fun f (Int) Int)
(push 1)
(assert (! (= x y) :named same))
(assert (! (distinct (f x) (f y)) :named different))
(check-sat)
(get-unsat-core)
(pop 1)
(check-sat)
(exit)
